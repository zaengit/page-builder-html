import{Section}from'../components/ui';import type{EditorContextValue}from'../types/editor';import{StyleField}from'./helpers';export function SizePanel({e}:{e:EditorContextValue}){return <Section title="Size">{[['Width','width'],['Height','height'],['Min width','minWidth'],['Max width','maxWidth'],['Min height','minHeight'],['Max height','maxHeight']].map(([l,p])=><StyleField key={p} e={e} label={l} prop={p}/>)}</Section>}