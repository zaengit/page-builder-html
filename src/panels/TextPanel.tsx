import{Section}from'../components/ui';import{setDirectText}from'../editor/mutations';import type{EditorContextValue}from'../types/editor';export function TextPanel({e}:{e:EditorContextValue}){const d=e.iframeRef.current?.contentDocument;const el=d&&e.selectedId?d.querySelector<HTMLElement>(`[data-vpb-id="${e.selectedId}"]`):null;const text=el?[...el.childNodes].filter(n=>n.nodeType===3).map(n=>n.textContent).join('').trim():'';return <Section title="Content"><textarea key={e.selectedId} defaultValue={text} onBlur={x=>e.mutate(el=>setDirectText(el,x.target.value))} className="min-h-20 w-full resize-y rounded-md border border-zinc-700 bg-zinc-950 p-2 text-xs text-zinc-200 outline-none focus:border-blue-500"/></Section>}