import{Field,Input,Section,Select}from'../components/ui';import type{EditorContextValue}from'../types/editor';export function LinkPanel({e}:{e:EditorContextValue}){const a=e.analysis?.attributes||{};return <Section title="Link"><Field label="URL"><Input key={a.href} defaultValue={a.href||''} onBlur={x=>e.mutate(el=>el.setAttribute('href',x.target.value))}/></Field><Field label="Target"><Select value={a.target||'_self'} onChange={x=>e.mutate(el=>el.setAttribute('target',x.target.value))}><option value="_self">Same tab</option><option value="_blank">New tab</option></Select></Field></Section>}