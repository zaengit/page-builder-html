import type{ButtonHTMLAttributes,InputHTMLAttributes,ReactNode,SelectHTMLAttributes}from'react';export const btn='inline-flex h-8 items-center justify-center gap-1.5 rounded-md border border-zinc-700 bg-zinc-900 px-2.5 text-xs font-medium text-zinc-200 transition hover:bg-zinc-800 disabled:opacity-40 focus:outline-none focus:ring-2 focus:ring-blue-500';export function Button(p:ButtonHTMLAttributes<HTMLButtonElement>){return <button {...p} className={`${btn} ${p.className||''}`}/>}export function Section({title,children}:{title:string;children:ReactNode}){return <section className="border-b border-zinc-800 p-3"><h3 className="mb-3 text-[11px] font-semibold uppercase tracking-wider text-zinc-500">{title}</h3><div className="space-y-2.5">{children}</div></section>}export function Field({label,children}:{label:string;children:ReactNode}){return <label className="grid grid-cols-[90px_1fr] items-center gap-2 text-xs text-zinc-400"><span>{label}</span>{children}</label>}export const input='h-8 min-w-0 rounded-md border border-zinc-700 bg-zinc-950 px-2 text-xs text-zinc-200 outline-none focus:border-blue-500';export function Input(p:InputHTMLAttributes<HTMLInputElement>){return <input {...p} className={`${input} ${p.className||''}`}/>}export function Select(p:SelectHTMLAttributes<HTMLSelectElement>){return <select {...p} className={`${input} ${p.className||''}`}/>}