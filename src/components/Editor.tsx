import{useEffect,useState}from'react';import{Layers3,SlidersHorizontal,X}from'lucide-react';import{Toolbar}from'./Toolbar';import{Canvas}from'./Canvas';import{ElementTree}from'./ElementTree';import{PropertiesPanel}from'./PropertiesPanel';import{useEditor}from'../hooks/useEditor';
export function Editor(){const e=useEditor();const[mobilePanel,setMobilePanel]=useState<'elements'|'properties'|null>(null);useEffect(()=>{const key=(x:KeyboardEvent)=>{const mod=x.ctrlKey||x.metaKey;if(mod&&x.key.toLowerCase()==='z'){x.preventDefault();x.shiftKey?e.redo():e.undo()}else if(x.key==='Delete'&&e.selectedId){x.preventDefault();e.remove()}};window.addEventListener('keydown',key);return()=>window.removeEventListener('keydown',key)},[e]);return <div className="flex h-dvh flex-col overflow-hidden bg-zinc-950 text-zinc-200"><Toolbar e={e}/><div className="relative flex min-h-0 flex-1"><ElementTree e={e} className="hidden md:block"/><Canvas e={e}/><PropertiesPanel e={e} className="hidden lg:block"/>
<div className="pointer-events-none absolute inset-x-0 bottom-3 z-30 flex justify-center gap-2 px-3 md:hidden"><button onClick={()=>setMobilePanel('elements')} className="pointer-events-auto inline-flex h-10 items-center gap-2 rounded-full border border-zinc-700 bg-zinc-900/95 px-4 text-xs font-medium shadow-xl backdrop-blur"><Layers3 size={15}/>Elements</button><button onClick={()=>setMobilePanel('properties')} className="pointer-events-auto inline-flex h-10 items-center gap-2 rounded-full border border-zinc-700 bg-zinc-900/95 px-4 text-xs font-medium shadow-xl backdrop-blur"><SlidersHorizontal size={15}/>Properties</button></div>
{mobilePanel&&<div className="absolute inset-0 z-40 bg-black/55 md:hidden" onClick={()=>setMobilePanel(null)}><div className="absolute inset-x-0 bottom-0 max-h-[72%] min-h-[280px] overflow-hidden rounded-t-2xl border-t border-zinc-700 bg-zinc-950 shadow-2xl" onClick={x=>x.stopPropagation()}><div className="flex h-11 items-center justify-between border-b border-zinc-800 px-3"><span className="text-xs font-semibold uppercase tracking-wider text-zinc-400">{mobilePanel}</span><button onClick={()=>setMobilePanel(null)} className="rounded-md p-2 text-zinc-400 hover:bg-zinc-800 hover:text-white" aria-label="Close panel"><X size={16}/></button></div>{mobilePanel==='elements'?<ElementTree e={e} className="h-[calc(72vh-44px)] w-full border-r-0"/>:<PropertiesPanel e={e} className="h-[calc(72vh-44px)] w-full border-l-0"/>}</div></div>}</div></div>}
