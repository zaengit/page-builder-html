import{useEffect,useState}from'react';import{Layers3,SlidersHorizontal,X}from'lucide-react';import{Toolbar}from'./Toolbar';import{Canvas}from'./Canvas';import{ElementTree}from'./ElementTree';import{PropertiesPanel}from'./PropertiesPanel';import{useEditor}from'../hooks/useEditor';
export function Editor(){const e=useEditor();const[mobilePanel,setMobilePanel]=useState<'elements'|'properties'|null>(null);const[showElements,setShowElements]=useState(true);const[showProperties,setShowProperties]=useState(true);useEffect(()=>{const key=(x:KeyboardEvent)=>{const mod=x.ctrlKey||x.metaKey;if(mod&&x.key.toLowerCase()==='z'){x.preventDefault();x.shiftKey?e.redo():e.undo()}else if(x.key==='Delete'&&e.selectedId){x.preventDefault();e.remove()}};window.addEventListener('keydown',key);return()=>window.removeEventListener('keydown',key)},[e]);return <div className="flex h-dvh min-h-0 flex-col overflow-hidden bg-zinc-950 text-zinc-200"><Toolbar e={e} showElements={showElements} showProperties={showProperties} onToggleElements={()=>setShowElements(v=>!v)} onToggleProperties={()=>setShowProperties(v=>!v)}/><div className="relative flex min-h-0 flex-1 overflow-hidden">{showElements&&<ElementTree e={e} className="hidden md:block"/>}<Canvas e={e}/>{showProperties&&<PropertiesPanel e={e} className="hidden lg:block"/>}
<div className="pointer-events-none absolute inset-x-0 bottom-[max(0.75rem,env(safe-area-inset-bottom))] z-30 flex justify-center gap-2 px-3 md:hidden"><button onClick={()=>setMobilePanel('elements')} className="pointer-events-auto inline-flex h-10 items-center gap-2 rounded-full border border-zinc-700 bg-zinc-900/95 px-4 text-xs font-medium shadow-xl backdrop-blur"><Layers3 size={15}/>Elements</button><button onClick={()=>setMobilePanel('properties')} className="pointer-events-auto inline-flex h-10 items-center gap-2 rounded-full border border-zinc-700 bg-zinc-900/95 px-4 text-xs font-medium shadow-xl backdrop-blur"><SlidersHorizontal size={15}/>Properties</button></div>
{mobilePanel&&<div className="fixed inset-0 z-40 flex items-end bg-black/55 md:hidden" onClick={()=>setMobilePanel(null)}><div className="flex h-[min(72dvh,calc(100dvh-3rem))] min-h-0 w-full flex-col overflow-hidden rounded-t-2xl border-t border-zinc-700 bg-zinc-950 pb-[env(safe-area-inset-bottom)] shadow-2xl" onClick={x=>x.stopPropagation()}><div className="flex h-11 shrink-0 items-center justify-between border-b border-zinc-800 px-3"><span className="text-xs font-semibold uppercase tracking-wider text-zinc-400">{mobilePanel}</span><button onClick={()=>setMobilePanel(null)} className="rounded-md p-2 text-zinc-400 hover:bg-zinc-800 hover:text-white" aria-label="Close panel"><X size={16}/></button></div><div className="min-h-0 flex-1 overflow-hidden">{mobilePanel==='elements'?<ElementTree e={e} className="h-full min-h-0 w-full border-r-0"/>:<PropertiesPanel e={e} className="h-full min-h-0 w-full max-w-none border-l-0"/>}</div></div></div>}</div></div>}
