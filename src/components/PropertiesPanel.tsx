import{Copy,Trash2,ArrowUp,ArrowDown}from'lucide-react';import{Button}from'./ui';import{TextPanel}from'../panels/TextPanel';import{TypographyPanel}from'../panels/TypographyPanel';import{ImagePanel}from'../panels/ImagePanel';import{SpacingPanel}from'../panels/SpacingPanel';import{SizePanel}from'../panels/SizePanel';import{BackgroundPanel}from'../panels/BackgroundPanel';import{BorderPanel}from'../panels/BorderPanel';import{LinkPanel}from'../panels/LinkPanel';import type{EditorContextValue,ElementCapability}from'../types/editor';export function PropertiesPanel({e}:{e:EditorContextValue}){const c=e.analysis?.capabilities||[];const has=(x:ElementCapability)=>c.includes(x);return <aside className="w-72 shrink-0 overflow-auto border-l border-zinc-800 bg-zinc-950"><div className="sticky top-0 z-10 border-b border-zinc-800 bg-zinc-950 p-3"><div className="flex items-center justify-between"><div><div className="text-[11px] font-semibold uppercase tracking-wider text-zinc-500">Properties</div>{e.analysis&&<div className="mt-1 text-xs text-zinc-300">&lt;{e.analysis.tagName}&gt; · {e.analysis.type}</div>}</div><div className="flex gap-1"><Button onClick={()=>e.move(-1)} title="Move up"><ArrowUp size={12}/></Button><Button onClick={()=>e.move(1)} title="Move down"><ArrowDown size={12}/></Button><Button onClick={e.duplicate} title="Duplicate"><Copy size={12}/></Button><Button onClick={e.remove} title="Delete"><Trash2 size={12}/></Button></div></div></div>{!e.analysis?<div className="p-5 text-center text-xs text-zinc-600">Select an element to edit its properties.</div>:<>{has('text')&&<TextPanel e={e}/>} {has('image')&&<ImagePanel e={e}/>} {has('href')&&<LinkPanel e={e}/>} {(has('typography')||has('color'))&&<TypographyPanel e={e}/>} {has('size')&&<SizePanel e={e}/>} {has('spacing')&&<SpacingPanel e={e}/>} {(has('background')||has('backgroundImage'))&&<BackgroundPanel e={e}/>} {has('border')&&<BorderPanel e={e}/>}</>}</aside>}