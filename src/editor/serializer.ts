export function serialize(doc:Document){const clone=doc.documentElement.cloneNode(true)as HTMLElement;[clone,...clone.querySelectorAll<HTMLElement>('*')].forEach(el=>[...el.attributes].forEach(a=>{if(a.name.startsWith('data-vpb-'))el.removeAttribute(a.name)}));clone.querySelectorAll('style[data-vpb-ui]').forEach(el=>el.remove());const dt=doc.doctype?`<!DOCTYPE ${doc.doctype.name}${doc.doctype.publicId?` PUBLIC "${doc.doctype.publicId}"`:''}${doc.doctype.systemId?` "${doc.doctype.systemId}"`:''}>`:'<!DOCTYPE html>';return `${dt}\n${clone.outerHTML}`}
export function downloadHtml(html:string,name:string){const url=URL.createObjectURL(new Blob([html],{type:'text/html;charset=utf-8'}));const a=document.createElement('a');a.href=url;a.download=name||'edited.html';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}