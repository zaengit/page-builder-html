import{ID}from'./selection';export function serialize(doc:Document,doctype='<!DOCTYPE html>'){const clone=doc.documentElement.cloneNode(true)as HTMLElement;clone.querySelectorAll(`[${ID}]`).forEach(el=>el.removeAttribute(ID));clone.removeAttribute(ID);clone.querySelectorAll('style[data-vpb-ui]').forEach(el=>el.remove());return `${doctype}\n${clone.outerHTML}`}
export function downloadHtml(html:string,name:string){const url=URL.createObjectURL(new Blob([html],{type:'text/html;charset=utf-8'}));const a=document.createElement('a');a.href=url;a.download=name||'edited.html';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}