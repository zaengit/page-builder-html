import{useCallback,useRef,useState}from'react';import{analyzeElement}from'../editor/elementAnalyzer';import{buildTree,byId,indexDocument,ID}from'../editor/selection';import{deleteElement,duplicateElement,moveElement}from'../editor/mutations';import{downloadHtml,serialize}from'../editor/serializer';import{useHistory}from'./useHistory';import type{Device,EditorContextValue,TreeNode,ElementAnalysis}from'../types/editor';
export function useEditor():EditorContextValue{const iframeRef=useRef<HTMLIFrameElement>(null);const history=useHistory();const[html,setHtml]=useState('');const[fileName,setFileName]=useState('edited.html');const[selectedId,setSelectedId]=useState<string|null>(null);const[analysis,setAnalysis]=useState<ElementAnalysis|null>(null);const[tree,setTree]=useState<TreeNode[]>([]);const[device,setDevice]=useState<Device>('desktop');const[,tick]=useState(0);const doc=()=>iframeRef.current?.contentDocument||null;const refresh=useCallback(()=>{const d=doc();if(!d)return;setTree(buildTree(d));const el=byId(d,selectedId);setAnalysis(el?analyzeElement(el):null);tick(x=>x+1)},[selectedId]);const snapshot=()=>{const d=doc();if(d)history.push({html:d.documentElement.outerHTML,selectedId})};const restore=(s:{html:string;selectedId:string|null}|null)=>{const d=doc();if(!d||!s)return;d.open();d.write(s.html);d.close();indexDocument(d);setSelectedId(s.selectedId);setTimeout(refresh)};const loadFile=async(f:File)=>{const text=await f.text();setHtml(text);setFileName(f.name);setSelectedId(null)};const select=(id:string|null)=>{setSelectedId(id);const d=doc();const el=d&&byId(d,id);setAnalysis(el?analyzeElement(el):null)};const mutate=(fn:(el:HTMLElement)=>void)=>{const d=doc(),el=d&&byId(d,selectedId);if(!el)return;fn(el);snapshot();refresh()};const duplicate=()=>mutate(el=>{const c=duplicateElement(el);c.querySelectorAll(`[${ID}]`).forEach(x=>x.removeAttribute(ID));c.removeAttribute(ID);indexDocument(el.ownerDocument);setSelectedId(c.getAttribute(ID))});const remove=()=>mutate(el=>{if(deleteElement(el))setSelectedId(null)});const move=(dir:-1|1)=>mutate(el=>moveElement(el,dir));const undo=()=>restore(history.undo());const redo=()=>restore(history.redo());const exportHtml=()=>{const d=doc();if(d)downloadHtml(serialize(d),fileName)};return{iframeRef,html,fileName,selectedId,analysis,tree,device,canUndo:history.canUndo,canRedo:history.canRedo,loadFile,select,setDevice,mutate,duplicate,remove,move,undo,redo,exportHtml,refresh}}